# Rust
/target
# Cargo.lock IS committed — this workspace ships binaries.

# Node
node_modules/

# Extension build output + bundled sidecar
extension/out/
extension/bin/

# Code-OSS build artifacts (the submodule tree is managed by scripts/bootstrap.sh)
/vscode/product.json.orig
/VSCode-*
/.build/

# Core workspace data (a Core run against the repo root drops this; the
# bridge integration tests spawn `valyria serve`)
/.valyria/

# OS / editor
.DS_Store
