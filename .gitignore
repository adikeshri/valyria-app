# Rust
/target
# Cargo.lock IS committed — this workspace ships binaries.

# Node
node_modules/

# Extension build output + bundled sidecar
extension/out/
extension/bin/

# Code-OSS build artifacts (the submodule tree is managed by scripts/bootstrap.sh)
/vscode/product.json.orig
/VSCode-*
/.build/

# Core workspace data (a Core run against any directory in this repo drops
# this — e.g. `valyria serve` invoked with `extension/` as its workspace
# during manual bridge testing, not just the repo root — so this must match
# at any depth, not just anchored to the root).
.valyria/

# OS / editor
.DS_Store
.vscode/
