## Two layouts, one switch

**Agent layout** puts the Valyria surfaces at the centre — you land on **Home**,
the activity bar steps out of the way, and files open beside the task as a
detail pane. **Editor layout** is the familiar VS Code shape.

Switch any time from the status bar or with `⌘K ⌘L`. Your choice is remembered
per repository.
