## Opening a repository is an authorization

The folder you open is the only path Valyria reads or writes. Core runs as a
supervised daemon scoped to that repository and keeps running even if this
window closes.
