## Evidence, not badges

The Verification view renders Core's `verified[]` / `unverified[]` exactly.
A category with no evidence reads **NOT RUN**. Change ownership (agent vs. you)
comes from Core's ledger, never a guess.
