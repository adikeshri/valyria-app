{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SubtaskCompletedPayload",
  "description": "`subtask_completed` — projected onto the *parent's* event stream when a child task reaches a terminal state (M5).",
  "type": "object",
  "required": [
    "child_task_id",
    "final_state"
  ],
  "properties": {
    "child_task_id": {
      "type": "string"
    },
    "final_state": {
      "description": "`completed` | `failed` | `cancelled`.",
      "type": "string"
    }
  }
}
