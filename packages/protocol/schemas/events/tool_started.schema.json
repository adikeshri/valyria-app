{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ToolStartedPayload",
  "description": "`tool_started` — a tool invocation began. `tool_invocation_id` pairs this with the matching `tool_completed` (G14).",
  "type": "object",
  "required": [
    "input",
    "tool",
    "tool_invocation_id"
  ],
  "properties": {
    "input": {
      "description": "The tool's canonical input JSON — shape is per-tool, left open."
    },
    "tool": {
      "type": "string"
    },
    "tool_invocation_id": {
      "type": "string"
    }
  }
}
