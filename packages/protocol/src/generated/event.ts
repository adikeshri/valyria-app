/* GENERATED by scripts/codegen.mjs from packages/protocol/schemas/.
 * Do not edit. Regenerate on a Core bump (see docs/INTEGRATION.md §3). */

/**
 * A projected event (§43), independent of transport. `kind`/`payload` mirror `valyria_events::EventEnvelope` — deliberately loose-typed (`kind` as a string, `payload` as raw JSON) since new event kinds and payload shapes are added by many crates over the life of the project, and the wire schema shouldn't need a breaking change every time one is.
 */
export interface WireEvent {
  kind: string;
  payload: unknown;
  seq: number;
  task_id?: string | null;
  ts_ms: number;
}
